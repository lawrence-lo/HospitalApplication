@model HospitalApplication.Models.ViewModels.NewDonation
@{
    ViewBag.Title = "Create";
}

<div class="row">
    <div class="col-sm-6 col-sm-offset-3">
        <h2 class="text-center">New Donation</h2>
        <form class="form-horizontal" action="/Donation/Create" method="POST">

            <div class="form-group">
                <label class="col-sm-4 control-label" for="DonationDescription">Donor</label>
                <div class="col-sm-8">
                    <select class="form-control" name="DonorID">

                        @foreach (var donor in Model.DonorOptions)
                        {
                            <option class="form-control" value="@donor.DonorID">@donor.DonorName</option>
                        }
                    </select>
                </div>
            </div>

            <div class="form-group">
                <label class="col-sm-4 control-label" for="DonationDescription">Department</label>
                <div class="col-sm-8">
                    <select class="form-control" name="DeptID">

                        @foreach (var department in Model.DepartmentOptions)
                        {
                            <option class="form-control" value="@department.DeptID">@department.DeptName</option>
                        }
                    </select>
                </div>
            </div>
            <div class="form-group">
                <label class="col-sm-4 control-label" for="DonationAmount">Amount</label>
                <div class="col-sm-8">
                    <input class="form-control" type="number" id="DonationAmount" name="DonationAmount">
                </div>
            </div>
            <div class="form-group">
                <label class="col-sm-4 control-label" for="DonationDate">Date</label>
                <div class="col-sm-8">
                    <input class="form-control" type="date" id="DonationDate" name="DonationDate" value="@System.DateTime.Now.ToString("yyyy-MM-dd")">
                </div>
            </div>
            <div class="form-group">
                <label class="col-sm-4 control-label" for="DonationDescription">Description</label>
                <div class="col-sm-8">
                    <textarea class="form-control" type="text" id="DonationDescription" name="DonationDescription" rows="5"></textarea>
                </div>
            </div>


            <div class="form-group">
                <div class="col-sm-offset-4 col-sm-8">
                    <button class="btn btn-default" type="submit">Add Donation</button>
                </div>
            </div>
        </form>
    </div>
</div>